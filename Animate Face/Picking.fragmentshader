#version 330 core

uniform vec4 vs_vertexColor;

// Ouput data
out vec4 color;

// Values that stay constant for the whole mesh.

void main(){

	color = vs_vertexColor;
	//color = vec4(PickingColor, PickingColor2, PickingColor3, 1.0);

}
